# Используем официальный образ Node.js в качестве базового для разработки
FROM node:20

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем файлы package.json и package-lock.json (если есть)
COPY package*.json ./

# Устанавливаем все зависимости, включая dev-зависимости
RUN npm install

# Копируем оставшуюся часть исходного кода приложения
COPY . .

# Открываем порт, который используется Webpack Dev Server (обычно 3000)
EXPOSE 3000

# Запуск сервера разработки
CMD ["npm", "run", "start"]
